"""senselab Label Studio ML backends.

Serves senselab audio analyzers (diarization first) as Label Studio ML backends. Named
``senselab_ls`` rather than ``label_studio_ml`` so it does not shadow the Label Studio ML
SDK's own top-level import.
"""
